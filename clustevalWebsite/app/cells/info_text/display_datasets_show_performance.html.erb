<p>Here you see a comparison matrix how well different clustering methods (rows) perform on this dataset. Where the performance is measured with different clustering quality measures (columns).</p>
<p>You can reorder the matrix by a certain column, to see on which clustering method performs best on this dataset, when measured by the corresponding clustering quality measure of that column.</p>