Counts how often a pair of objects is clustered together in the best clustering of each clustering method on a data input. These sets of clusterings originate from parameter optimizations, when the same clustering method is applied to the same data set using different density parameter values.