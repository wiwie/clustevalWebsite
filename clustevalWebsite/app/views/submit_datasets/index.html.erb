<h3>You want to submit a dataset to our platform?</h3>
Then fill in your details below and we will send you an email with further instructions.<br />
Required field: (*) <br /><br />
<%= form_for(@submit_dataset) do |f| %>
	<%= f.error_messages %>
	<%= f.label :email, 'E-mail (*)' %>
	<%= f.text_field :email %><br />
	<%= f.label :firstname, 'First name (*)' %>
	<%= f.text_field :firstname %><br />
	<%= f.label :lastname, 'Last name (*)' %>
	<%= f.text_field :lastname %><br />
	<%= f.label :institution, 'Institution (*)' %>
	<%= f.text_field :institution %><br />
	<%= f.label :description, 'Description (*)' %><br />
	<%= f.text_area :description %><br />
	<%= f.submit 'Submit' %>
<% end %>