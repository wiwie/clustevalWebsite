<% content_for :title, "How well do clustering methods perform on '" + DatasetConfig.find(@dataset_id).name + "'?" %>
<%= render_cell :small_ranking, :ds_and_q, :obj => DatasetConfig.find_by_id(@dataset_id), :iterationsExts => ParameterOptimizationIterationsExt.select("program_id,dataset_config_id,max(quality) as maxQuality,min(quality) as minQuality,paramSetAsString,clustering_quality_measure_id").where(:dataset_config_id => @dataset_id).group("program_id,clustering_quality_measure_id") %>