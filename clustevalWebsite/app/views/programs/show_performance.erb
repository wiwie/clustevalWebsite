<% content_for :title, "How well does '" + @program.name + "' perform on different datasets?" %>
<%= cell(:small_ranking).(:p_and_q, :obj => @program, :iterationsExts => ParameterOptimizationIterationsExt.select("dataset_id,max(quality) as max_quality,min(quality) as min_quality,clustering_quality_measure_id").where(:program_id => @program.id).group("dataset_id,clustering_quality_measure_id")) %>